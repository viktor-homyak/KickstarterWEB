<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"

       xsi:schemaLocation="http://www.springframework.org/schema/beans
	   					   http://www.springframework.org/schema/beans/spring-beans.xsd
	   					   http://www.springframework.org/schema/tx
                           http://www.springframework.org/schema/tx/spring-tx.xsd
                           http://www.springframework.org/schema/context
                           http://www.springframework.org/schema/context/spring-context.xsd">




    <context:property-placeholder location="classpath:config.properties"/>

    <bean id="dataSource" class="org.apache.tomcat.jdbc.pool.DataSource" destroy-method="close">
        <property name="driverClassName" value="${driver}"/>
        <property name="url" value="${url}"/>
        <property name="username" value="${user}"/>
        <property name="password" value="${password}"/>
        <property name="initialSize" value="5"/>
        <property name="maxActive" value="10"/>
        <property name="maxIdle" value="5"/>
        <property name="minIdle" value="2"/>
    </bean>

    <bean id="sessionFactory" class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="packagesToScan" value="ua.com.goit.homyak.mvc.model"/>
        <!--<property name="namingStrategy" ref="namingStrategy"/>-->
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">${db.hibernate.dialect}</prop>
                <prop key="hibernate.show_sql">${db.hibernate.show_sql}</prop>
                <prop key="hibernate.format_sql">${db.hibernate.format_sql}</prop>
                <prop key="hibernate.hbm2ddl.auto">${db.hibernate.hbm2ddl.auto}</prop>
            </props>
        </property>
    </bean>
    <!--<bean id="namingStrategy" class="org.hibernate.cfg.ImprovedNamingStrategy"/>-->



    <bean id="transactionManager" class="org.springframework.orm.hibernate4.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>
    <tx:annotation-driven/>

    <bean id="quoteGenerator" class="ua.com.goit.homyak.dao.QuoteGenerator"/>

    <bean id="categoryDAOPS" class="ua.com.goit.homyak.dao.CategoryPostgreSQLDAO">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>

    <bean id="projectDAOPS" class="ua.com.goit.homyak.dao.ProjectPostgreSQLDAO">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>

    <bean id="mainServlet" class="ua.com.goit.homyak.servlet.MainServlet">
        <property name="quote" ref="quoteGenerator"/>
        <property name="categoryDAO" ref="categoryDAOPS"/>
        <property name="projectDAO" ref="projectDAOPS"/>
    </bean>






</beans>